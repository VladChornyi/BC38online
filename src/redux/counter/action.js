// import { createAction } from '@reduxjs/toolkit';
// import { nanoid } from 'nanoid';

// export const incrementAction = (payload = 1) => {
//   return { type: INCREMENT, payload };
// };
// export const incrementAction = createAction('counter/increment', (number = 1) => ({
//   payload: {
//     number,
//     id: nanoid(),
//   },
// }));

// export const decrementAction = (payload = 1) => {
//   return { type: DECREMENT, payload };
// };
// export const decrementAction = createAction('counter/decrement');
